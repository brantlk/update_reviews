============
Installation
============

At the command line::

    $ pip install update_reviews

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv update_reviews
    $ pip install update_reviews
